# 需求会话：工作环境初始化

## 状态

- 状态：完成
- 日期：2026-08-19

## 需求

- 在 `D:\charlie\COH2` 展开工作。
- 按全局基础规范建立项目规则体系。

## 变更记录

- 完整读取 `D:\charlie\Workbencch\spec\rules-bootstrap-spec.md`。
- 检查项目目录，确认初始化前为空且不存在可合并的同类规则文件。
- 检索项目 `mistakes/`（初始化前不存在），随后读取 Workbencch 错题目录说明。
- 建立规则、会话、错题、规范和术语表骨架。

## 验证

- 目录与必需文档存在。
- 规则包含角色、回复前缀、变更控制、错题检索与规则同步要求。

## 继承指引

- 后续任务先读 `../session.md`、`../../rules/rules.md`，再检索 `../../mistakes/`。
- 首个具体需求应新建独立的需求会话文档，不在本文件继续追加需求设计。
